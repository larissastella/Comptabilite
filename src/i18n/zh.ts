// PLACEHOLDER — pending real zh translation. Content is temporarily a
// straight copy of fr.ts so the language selector works and no key ever
// renders blank or falls through to a raw i18next key while this
// language is being translated string-by-string in follow-up work. Once
// this file has its own real content, this comment (and the import
// below) should be removed.
import fr from './fr';

export default fr;
